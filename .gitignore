# Claude Code internal files
.claude/

# Runtime secrets
data/.fr24_cookies.pkl
data/.secret_key

# Personal deployment/local-only scripts — not for the public repo
*.ps1
!studio/run.ps1


# This project's own Docker deployment config and Claude Code project notes
# — not for the public repo
Dockerfile
docker-compose.yml
CLAUDE.md

# Runtime downloaded assets — fetched on demand, not committed
static/airline_logos/

# Runtime database in data/ — static JSON files in data/ are tracked
data/*.db
data/*.db-shm
data/*.db-wal
data/backups/

# Local pre-migration backups (DB dumps pulled from the deployed server)
/backups/

# Logs
logs/

# Personal planning notes and docs
to_do.md
docs/

# Lightroom catalog (personal data, large binary)
lightroom/*.lrcat
lightroom/*.lrcat-journal
lightroom/*.lrcat-shm
lightroom/*.lrcat-wal

# Catalog working folder (drop .lrcat here, get cleaned .lrcat out)
catalog/

# Utility scripts folder — not for the public repo
scripts/

# SpotAlert Studio — local runtime data (thumbnails cache, inbox/output test photos, its own log)
studio/data/thumbnails/
studio/data/inbox/
studio/data/output/
studio/server.log

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/
.venv/
venv/
